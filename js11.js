// boolean and comparsion operator

// booleans
//  true,false

let num1="7";
let num2=7;

//  < , > .

// == vs ===
console.log(num1 == num2); //it will give true because == operator only compare the value not type.

console.log(num1 === num2); //it will give false because === operator compare both value and type.
// != and !==
// != compare only value .
// !== compare both value and type.
